fluidPage(
    fluidRow(
        box(# height = "600px",
            title = "About",
            width = 12,
            # status = NULL, 
            # closable = FALSE,
            # maximizable = FALSE, 
            collapsible = FALSE,
            includeHTML("ABOUT.html")
        )
    )
)
