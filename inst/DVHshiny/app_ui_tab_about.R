fluidPage(
    fluidRow(
        bs4Box(
            # inputId = "card_about_1",
            # height = "600px",
            title = "About",
            width = 12,
            # status = NULL, 
            # closable = FALSE,
            # maximizable = FALSE, 
            # collapsible = TRUE,
            includeHTML("ABOUT.html")
        )
    )
)
